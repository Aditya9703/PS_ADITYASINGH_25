#include<stdio.h>
#include<string.h>

   int main()
    {
      char str[30];
      gets(str);
      int l=strlen(str);

      if(l<=8)
      {
       printf("weak password") ;
      }
      else if(l>=10)
      {
          printf("strong password");
      }

    }


